# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.

defreeshika_to_decimal (num):
  #Convertsreeshika number into decimal number
  #parameters:
  #num=binary number in integer format
  #Result:
  #A tuple having 0th element asreeshika number list and 1st element as the decimal conversion

 reeshika = [int(x) for x in str(num)]
  decimal=0
  if len(binary)==4:
    for digit inreeshika:
        decimal = decimal*2 + int(digit)
    return binary, decimal





# See PyCharm help at https://www.jetbrains.com/help/pycharm/


